<%= render 'layouts/navbar' %>

<% if @users.any? %>
  <div class="container mt-5">
    <hr class="border border-dark" style="height: 8px; background-color: black; margin-bottom: 3rem">
    <div data-controller="filter">
      <form class="d-flex mb-2">
        <input class="form-control me-2" type="search" placeholder="Search by name" aria-label="Search" data-filter-target="input" data-action="input->filter#filter">
      </form>
      <div class="row">
        <% @users.each do |user| %>
          <div class="col-md-4 mb-4 user" data-filter-target="item">
            <div class="card bg-color-card">
              <div class="card-body">
                <h5 class="card-title"><%= user.name %></h5>
              </div>
              <div class="card-footer d-flex align-items-center gap-2">
                <%= link_to 'Add Friend', send_request_path(user), class: 'btn btn-success' %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
<% else %>
  <div class="container text-center mt-5">
    <p style="font-size: 48px;"><i class="far fa-frown"></i> There is no registered user <i class="far fa-frown"></i></p>
  </div>
<% end %>
