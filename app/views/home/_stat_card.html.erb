<%= link_to path, class: 'stat-card' do %>
  <span class="stat-card__icon stat-card__icon--<%= tone %>"><%= icon icon_name %></span>
  <span class="stat-card__info">
    <span class="stat-card__value"><%= value %></span>
    <span class="stat-card__label"><%= label %></span>
  </span>
<% end %>
